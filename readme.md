todos:
[ ] Project management/file folder management (for books with chapters)
    [x] a view showing the Project hierarchy
    [x] by clicking each chapter (an abstraction, not an individual file), the Chinese and English texts should be loaded to the grids side by side. The other chapters should be wrapped.
    [x] Be able to load a "Project", instead of multiple separated files
    [ ] "Search" function: search globally in the project, instead of in the opened and rendered individual file.
[ ] large file management
    1. If there are too many lines, render tens of ten at once only (Virtual Scrolling)
[ ] Enable comments 
    [ ] first do it locally
    [ ] later I should host a server. By granting each user an access code and an id, they should be able to comment at text-line-level.
[ ] enable backup translations

Bug: reverse sync (local file -> tool) stats not displaying correctly. (watcher's problem)