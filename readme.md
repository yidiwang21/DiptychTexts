todos:
1. Project management/file folder management (for books with chapters)
    1. a view showing the Project hierarchy
    1. by clicking each chapter (an abstraction, not an individual file), the Chinese and English texts should be loaded to the grids side by side. The other chapters should be wrapped.
    1. Be able to load a "Project", instead of multiple separated files
    1. "Search" function: search globally in the project, instead of in the opened and rendered individual file.
1. large file management
    1. If there are too many lines, render tens of ten at once only (Virtual Scrolling)
1. Enable comments 
    1. first do it locally
    1. later I should host a server. By granting each user an access code and an id, they should be able to comment at text-line-level.

Bug: project name not loaded
Bug: reverse sync (local file -> tool) stats not displaying correctly. (watcher's problem)